# ハンズオン 背景・方針

## 背景

プログラミング初心者を対象に、GoでのWebAPI開発の基礎を体験してもらうためのハンズオン教材です。
「動くものを作る」体験を通じて、プログラミングの楽しさと実用性を伝えることを目的としています。

## 対象者

- プログラミング経験がほとんどない方
- 他言語は少し触ったことがある方

前提知識は不要です。

## 構成方針

### 段階的なステップ

各ステップは前のステップの知識を使う設計にしています。
急に難しくなるポイントを作らず、一つ一つ積み上げていきます。

```
Hello, World（出力の基本）
  ↓
ループ（繰り返し処理）
  ↓
関数化（コードの整理）
  ↓
偶奇判定（条件分岐）
  ↓
関数 × ループの組み合わせ
  ↓
FizzBuzz（総合練習）
  ↓
REST API（実用的なアプリケーション）
```

### コードの書き方

- **標準ライブラリのみ使用**: 外部パッケージを極力使わず、Goの素の書き方を学ぶ
- **1ファイル完結**: 各ステップは `main.go` 1ファイルに収める。ファイル分割は扱わない
- **コメントで補足**: 初見で何をしているか分からない行にはコメントを付ける
- **余分な機能を入れない**: そのステップで伝えたいこと以外は入れない

### 意図的に省いたもの

初心者が最初に躓きやすい以下の要素は、後のステップに回しています。

| 省いた要素 | 理由 |
|---|---|
| エラーハンドリング (`if err != nil`) | まず動くものを見せる。エラー処理は別途 |
| `encoding/json` | `fmt.Fprintf` で十分な段階では不要 |
| メソッドチェック (`r.Method`) | GET以外のリクエストは最初の段階では考慮しない |
| テストコード | `httptest` パッケージは別の回に |

## APIサーバーの方針

REST APIの回からは `net/http` 標準パッケージを使います。
GinやEchoなどのフレームワークは使いません。まずGoの素のHTTP処理を理解してから、
必要に応じてフレームワークへ移行する流れを想定しています。
